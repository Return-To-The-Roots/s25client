"total time per call" = d("calls:*") / d("calls:*") * d("calltimes:*") / d("frames") * 1000
{
    precision 3
    label "* (ms)"
}

"total time" = d("calltimes:total") / d("frames") * 1000
{
    precision 3
    label "(ms)"
}

"number of calls" = d("calls:*") / d("calls:*") * d("calls:*") / d("frames")
{
    precision 0
    label "* calls"
}

"frames per second" = d("frames") / d("seconds")
{
    precision 1
    label "fps"
}
